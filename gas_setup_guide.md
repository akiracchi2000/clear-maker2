# Clear Maker2 管理者向けGAS連携マニュアル (非公開用)

このドキュメントは、Clear Maker2の管理者（配布者）向けに、生徒の利用データ（問題プロンプトやAIの回答など）を裏側でGoogleスプレッドシートに自動収集するための手順を解説したものです。
**※このファイルは生徒（利用者）には配布しないでください。**

## 1. Google スプレッドシート側の準備 (GASのデプロイ)
1. **スプレッドシートの作成**: 新しい Google スプレッドシート を作成します。シートの名前がデフォルトの「シート1」であることを確認してください。
2. **スクリプトエディタを開く**: 上部メニューの **[拡張機能] > [Apps Script]** をクリックします。
3. **コードの貼り付け**: 開いたエディタに、同梱されている `gas_template.gs` の内容をすべてコピーして貼り付け、保存（フロッピーアイコン または Ctrl+S）します。
4. **ウェブアプリとしてデプロイ**:
   - 画面右上の **[デプロイ] > [新しいデプロイ]** をクリックします。
   - 左側の歯車アイコン（種類の選択）で「**ウェブアプリ**」を選びます。
   - 以下の設定にします:
     - 実行するユーザー: 「自分」
     - アクセスできるユーザー: 「**全員**」
   - 右下の [デプロイ] をクリックします。（※Googleアカウントへのアクセス承認を求められたら「詳細」→「（安全ではないページ）に移動」から許可して進めてください）
5. **URLのコピー**: デプロイ完了画面に表示される「**ウェブアプリの URL**」をコピーして控えておきます。

## 2. アプリ (Clear Maker2) へのURLの埋め込み
設定画面などユーザーの目に触れる場所には入力欄がありません。**ソースコードの先頭に直接URLを埋め込みます。**

1. エディタ（VS Codeやメモ帳など）で `app.src.js` を開きます。
2. ファイルの2行目付近にある `const ADMIN_GAS_WEBHOOK_URL = '';` の部分を見つけます。
3. `''` の中に、先ほどコピーしたウェブアプリのURLを貼り付けます。
   - 例: `const ADMIN_GAS_WEBHOOK_URL = 'https://script.google.com/macros/s/AKfyc.../exec';`
4. 上書き保存します。

## 3. アプリのビルド (難読化) による隠蔽
`app.src.js` のまま配布するとURLが丸見えになってしまうため、難読化を行って `app.js` を生成します。

1. Pythonがインストールされている環境で、コマンドプロンプトやターミナルを開きます。
2. Clear Maker2のフォルダに移動（`cd`）します。
3. `python obfuscate.py` を実行します。
4. 新しく上書き生成された `app.js` を確認します。

## 4. 生徒への配布
生徒に配布する際は、以下のファイル・フォルダのみを渡すようにしてください。

**配布するもの:**
- `index.html`
- `style.css`
- `app.js` （難読化済みのもの）
- `sw.js`
- `manifest.json`
- アイコン画像 (`icon-192.png`, `icon-512.png`)
- （任意）`README.md` ※ここにはGASのことは書いてありません。

**配布してはいけないもの（削除または含めない）:**
- `app.src.js` （URLが書かれた元のソースコード）
- `obfuscate.py` （難読化ツール）
- `gas_template.gs` （GASのコード）
- `gas_setup_guide.md` （このマニュアルファイル）

以上で設定は完了です。生徒が解析を実行するたびに、裏側でスプレッドシートへデータが蓄積されます。
